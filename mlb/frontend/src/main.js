import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <div className="min-h-screen bg-red-500 text-white p-4 text-xl font-bold">
      ✅ Tailwind is working
    </div>
  </React.StrictMode>
);
