# Supporting model-family comparison

`MODEL_FAMILY_EVIDENCE = STRONG`

`EXACT_CURRENT_FORWARD_BEHAVIOR = MIXED_WITH_FAMILY_HISTORY`

The prior season-assembled review rated the broader family strong, with historical discrimination but a known high-tail calibration defect across older generations. Those generations are not observations of this exact artifact. The exact current artifact's prospective ordering is `ROBUST`, but it does not demonstrate population-baseline skill at this checkpoint; its separately assessed upper-tail status is `INSUFFICIENT_SAMPLE`. The historical defect is not inherited automatically, and the current result is mixed—not pooled—with family history.
