# Schedule-relative cohort V2

V1 is retired because a fixed 1:00 PM anchor is unsuitable for variable MLB schedules. Its August 1 empty cohort remains preserved but is not H1 evidence and does not count in V2.

V2 selects the latest existing wrapper 45–240 minutes before the earliest official first pitch. Each governing capture includes games 30–240 minutes away. The existing wrapper calls the default-enabled V2 hook at its normal times; non-selected calls stop without research writes. No LaunchAgent was added.

August 2 selected the 9:30 AM wrapper. Because implementation completed earlier while a valid untouched window remained, the authorized CLI-only emergency path captured at 8:45:53 AM PT, 109.11 minutes before the earliest game. It froze 89 baseline rows, 46 positions 1–3, and 43 positions 4–9 across eleven in-window games with zero identity rejects.

An initial technical publication used the source run-tag timestamp instead of the admitted same-run timestamp and incorrectly produced zero rows. It was immediately quarantined, excluded from the block, and preserved with an invalidation manifest. The corrected retry is the sole canonical August 2 cohort.

V2 block status: one attempted date, closeout pending. No H1 grading was performed.
