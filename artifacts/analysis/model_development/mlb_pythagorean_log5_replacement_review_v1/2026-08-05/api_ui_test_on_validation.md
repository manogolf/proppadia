# API/UI test-on validation

Test-only feature-on coverage verifies team-specific probabilities, winner and confidence rendering, historical validation/holdout disclosure, explicit score unavailability, required betting-edge disclosure, and absence of EV/wager output. Production feature flag remained off. The responsive grid remains one column on small screens and two on large screens. Load or identity failures remain unavailable and never fall back to the old baseline.
